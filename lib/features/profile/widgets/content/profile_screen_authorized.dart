part of '../profile_screen.dart';

class _ProfileScreenAuthorized extends StatelessWidget {
  final ProfileViewState state;

  const _ProfileScreenAuthorized({required this.state});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
